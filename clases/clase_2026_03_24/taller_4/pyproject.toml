[project]
name = "taller-evaluado-02"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.8"
dependencies = [
    "pycryptodome>=3.23.0",
    "pytest>=8.0.0,<9.0.0",
    "typer[all]>=0.20.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
]

[tool.ruff]
line-length = 130

[tool.ruff.lint]
select = ["E", "F", "N", "W"]
